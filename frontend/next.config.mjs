const nextConfig = {
  images: {
    remotePatterns: [{ hostname: "openweathermap.org" }, { hostname: "storage.googleapis.com" }, { hostname: "lh3.googleusercontent.com"}, { hostname: "flagsapi.com"}],
  },
};

export default nextConfig;
